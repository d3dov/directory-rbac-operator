---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: rbacgroupbindings.ldaprbac.io
spec:
  group: ldaprbac.io
  names:
    categories:
    - ldaprbac
    kind: RBACGroupBinding
    listKind: RBACGroupBindingList
    plural: rbacgroupbindings
    shortNames:
    - rgb
    singular: rbacgroupbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.providerRef
      name: Provider
      type: string
    - jsonPath: .spec.groupDN
      name: Group
      type: string
    - jsonPath: .spec.roleRef.name
      name: Role
      type: string
    - jsonPath: .status.memberCount
      name: Members
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.lastSyncTime
      name: Last Sync
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RBACGroupBinding maps an LDAP/AD group to a namespaced Role or ClusterRole
          by continuously reconciling a RoleBinding's subjects to the group's
          membership.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RBACGroupBindingSpec maps an LDAP/AD group onto a namespaced
              RoleBinding.
            properties:
              groupDN:
                description: GroupDN is the distinguished name of the source group.
                type: string
              providerRef:
                description: |-
                  ProviderRef names the LDAPProvider this binding resolves membership
                  against.
                type: string
              roleRef:
                description: RoleRef is the Role or ClusterRole the managed RoleBinding
                  grants.
                properties:
                  kind:
                    enum:
                    - Role
                    - ClusterRole
                    type: string
                  name:
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - groupDN
            - providerRef
            - roleRef
            type: object
          status:
            description: RBACGroupBindingStatus reports the last observed sync result.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                format: date-time
                type: string
              memberCount:
                description: |-
                  MemberCount is the number of members resolved on the last successful
                  sync.
                format: int32
                type: integer
              membersHash:
                description: |-
                  MembersHash is a hash of the full sorted member set, used internally to
                  detect drift without diffing large lists on every reconcile.
                type: string
              membersPreview:
                description: |-
                  MembersPreview is a capped, sorted sample of resolved member names, for
                  quick inspection without listing the managed RoleBinding.
                items:
                  type: string
                type: array
              membersTruncated:
                description: |-
                  MembersTruncated is true when MembersPreview omits members because the
                  full set exceeds the preview cap.
                type: boolean
              observedGeneration:
                format: int64
                type: integer
              roleBindingRef:
                description: RoleBindingRef names the RoleBinding this binding manages.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
